source "https://rubygems.org"

gem 'github-pages', '>= 228', group: :jekyll_plugins

group :jekyll_plugins do
   gem "jekyll-feed", ">= 0.17"
   gem 'jekyll-livereload', '>= 0.2.2'
   gem 'nokogiri', '>= 1.16'
   gem 'rexml', '>= 3.3.0'
   # Do not force a newer commonmarker here — github-pages (via jekyll-commonmark-ghpages)
   # depends on commonmarker ~> 0.23.7. Leave commonmarker to be resolved by github-pages
   # to avoid RubyGems version conflicts during bundle install.
end